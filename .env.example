# Copy this to .env and fill in values for local development.
# NEVER commit .env to git.

# URL of your deployed Cloudflare Worker (set after deploying the worker)
HIVE_WORKER_URL=https://tbc-sheets-worker.YOUR_SUBDOMAIN.workers.dev
